import React from "react";

import ScheduledEntry from "../components/functional/ScheduledEntry";

const ScheduledEntryPage = () => {
  return <ScheduledEntry />;
};

export default ScheduledEntryPage;
